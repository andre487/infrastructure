# Config is autogenerated – do not change it manually
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;

    server_name {{ clamav_domain }};

    ssl on;
    ssl_certificate_key /etc/letsencrypt/live/{{ clamav_domain }}/privkey.pem;
    ssl_certificate /etc/letsencrypt/live/{{ clamav_domain }}/fullchain.pem;

    location / {
        root /var/www/{{ clamav_domain }};
    }
}

server {
    listen 80;
    listen [::]:80;

    server_name {{ clamav_domain }};

    location / {
        root /var/www/{{ clamav_domain }};
    }
}
