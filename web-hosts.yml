- hosts: web_hosts
  become: true
  become_method: sudo
  environment:
    DEBIAN_FRONTEND: noninteractive

  vars:
    service_port: 8888

  roles:
    - system
    - firewall
    - logrotate
    - https-site
    - fail2ban
